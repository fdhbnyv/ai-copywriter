{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd server && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd server && gunicorn main:app --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/health"
  }
}
