services:
  - type: web
    name: ai-copywriter
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --timeout 120
    envVars:
      - key: ZHIPU_API_KEY
        sync: false
      - key: MANXIAOBAI_API_KEY
        sync: false
